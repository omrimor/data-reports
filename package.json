{
  "name": "data-reports",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.1.3",
    "lodash": "^4.17.11",
    "react": "^16.6.3",
    "react-bar-chart": "^0.1.2",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1",
    "reactstrap": "^6.5.0"
  },
  "scripts": {
    "start:mockapi": "json-server --watch db/db.json --port 4001",
    "start:dev": "react-scripts start",
    "start": "npm-run-all --parallel start:dev start:mockapi",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "json-server": "^0.14.0",
    "npm-run-all": "^4.1.5"
  }
}
